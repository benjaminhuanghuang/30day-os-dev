## Install NASM
```
  brew install nasm
```
compile
```
  nasm kernel.asm -o kernel.bat
```

## Install the virtual machine
Install qemu（quick emulator）

```
  brew install qemu
```

Run the os img
```
  qemu-system-x86_64
```